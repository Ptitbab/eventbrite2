<%= form_tag event_orders_path do %>
<article>
  <% if flash[:error].present? %>
    <div id="error_explanation">
      <p><%= flash[:error] %></p>
    </div>
  <% end %>

  <div class="card text-center">
  <div class="card-header">
    Trop cool ! Tu veux participer à cet évènement ? Bah paye habibi !
  </div>
  <div class="card-body">
    <h5 class="card-title">Recapitulatif de la commande :</h5>
    <p class="card-text">Participation à l'évènement <%= @event.title.capitalize %> : <%= @event.price%> €</p>
    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
    data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-description="Participation à l'evenement"
    data-amount="<%= @event.price * 100 %>"
    data-locale="fr">
    </script>
  </div>
  <div class="card-footer text-muted">
    Notre service est totalement securisé, aucun souci à te faire
  </div>
</div>


</article>

<% end %>